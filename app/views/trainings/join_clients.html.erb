<% if @clients.any? %>
  <% @clients.each do |client| %>
<!--        <div class="ui form raised very padded text containe" >-->
    <label><%= client.first_name + " " + client.second_name %></label>
    <div class="field" style="margin-bottom: 10px">
      <div class="ui labeled input">
        <div class="ui label">$</div>
        <input type="text" placeholder="<%= client.price %>">
      </div>
    </div>
<!--    </div>-->
  <% end %>
<% end %>